spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/relatorio_financeiro
    username: postgres
    password: univille

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

    database-platform: org.hibernate.dialect.PostgreSQLDialect